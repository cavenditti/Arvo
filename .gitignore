# Rust
backend/target/
# Node / Expo
app/node_modules/
app/.expo/
app/dist/
app/web-build/
# Env & local data
.env
app/.env.local
var/
# OS / editor
.DS_Store
*.log
# design source mocks (kept locally, not in the repo)
ui-mock.zip
