// OWNER: fe-map — Parcel create — draw on map or import GeoJSON, then crop/season form.
import { Text, View } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>parcel/new — to be implemented (fe-map)</Text>
    </View>
  );
}
