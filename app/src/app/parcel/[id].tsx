// OWNER: fe-map — Parcel detail — info, mini-map, IndexChart, WeatherPanel, alerts.
import { Text, View } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>parcel/[id] — to be implemented (fe-map)</Text>
    </View>
  );
}
