// Native stack route for the plant map. Keeping it outside NativeTabs lets the route remain
// reachable from a field without turning a secondary workspace into a permanent tab item.
export { default } from './(tabs)/plants';
