// OWNER: fe-map — Map — all parcels on Leaflet, tap to open detail, entry to parcel/new.
import { Text, View } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>(tabs)/map — to be implemented (fe-map)</Text>
    </View>
  );
}
