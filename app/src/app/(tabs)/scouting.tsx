// OWNER: fe-scouting — Scouting — observation feed with sync badges + new observation.
import { Text, View } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>(tabs)/scouting — to be implemented (fe-scouting)</Text>
    </View>
  );
}
