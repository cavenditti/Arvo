// OWNER: fe-dashboard — Dashboard — parcel cards with latest NDVI, sparkline, alert badges.
import { Text, View } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>(tabs)/index — to be implemented (fe-dashboard)</Text>
    </View>
  );
}
