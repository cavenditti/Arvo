// OWNER: fe-scouting — New observation — location, photos, tags, offline-first save.
import { Text, View } from 'react-native';

export default function Screen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>observation/new — to be implemented (fe-scouting)</Text>
    </View>
  );
}
